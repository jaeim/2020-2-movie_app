<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ddwc.mobile.finalProject.movie">


    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movie"
        android:usesCleartextTraffic="true">
        <!--tools:replace="android:allowBackup"-->

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/api_key" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ddwc.mobile.finalProject.movie.DetailMovieActivity"></activity>
        <activity android:name=".WatchedMovieActivity"></activity>
        <activity android:name=".FavoriteMovieActivity"></activity>
        <activity android:name=".BoxOfficeActivity"></activity>
        <activity android:name=".DetailWatchedMovieActivity"></activity>
        <activity android:name=".DetailFavoriteActivity"></activity>
        <activity android:name=".NearByTheaterActivity"></activity>
    </application>

</manifest>